# --- A. 讀取交接內容 ---
        raw_data = sheet_continuous.get_values(store['read_range']) # 👈 改成讀取專屬範圍
        
        # ... (中間發送 DC 邏輯不變) ...
        
        # --- C. 在當日表進行本地備援 ---
        sheet_daily.batch_clear(['A38:C50'])
        sheet_daily.update_acell('A38', '【昨日交接紀錄備份】')
        sheet_daily.update(store['backup_range'], raw_data) # 👈 改成寫入專屬範圍
        
        # --- D. 執行當日表的每日清除動作 ---
        sheet_daily.batch_clear(store['clear_ranges']) # 👈 改成清空專屬範圍
